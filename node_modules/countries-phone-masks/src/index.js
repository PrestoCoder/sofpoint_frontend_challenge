const countries = require('./countries.json')

const countriesMasks = countries.reduce((acc, obj) => {
    const { iso, ...remainingValues } = obj;
    acc[iso] = remainingValues;
    return acc;
}, {});
  

module.exports = {countriesMasks};
